version: '3.8'

services:
  mysql:
    container_name: like_hero_mysql
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: MyNewPass123!@#
      MYSQL_DATABASE: co2db
      MYSQL_USER: co2user
      MYSQL_PASSWORD: 123
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./src/main/resources/META-INF/init_users.sql:/docker-entrypoint-initdb.d/init_users.sql
    command:
      - --default-authentication-plugin=mysql_native_password
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "co2user", "-p123"]
      interval: 10s
      timeout: 5s
      retries: 5

  app:
    build: .
    container_name: co2_app
    ports:
      - "8080:8080"
    depends_on:
      mysql:
        condition: service_healthy
    environment:
      - CATALINA_OPTS=-Djava.security.egd=file:/dev/./urandom
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/"]
      interval: 30s
      timeout: 10s
      retries: 3

volumes:
  mysql_data: 